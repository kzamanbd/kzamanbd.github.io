import Form from './form';

const URLShortener = () => {
    return (
        <div className="radial-gradient flex h-screen items-center justify-center bg-gray-50 dark:bg-gray-900">
            <div className="w-full max-w-2xl">
                <Form />
            </div>
        </div>
    );
};

export default URLShortener;
